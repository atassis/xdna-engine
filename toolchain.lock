# toolchain.lock -- the single source of truth for ONE exact AIE toolchain.
# Sourced by scripts/toolchain_up.sh. Change this file = change the toolchain (deliberately).
# Phase 2: mlir-aie built from a CLEAN atassis/mlir-aie fork integration-branch commit (no dirty working tree),
# with the route_b kernels overlaid by sync_kernels.sh (policy B). The base 8373e49 + our 14-patch stack are
# the commits ON this branch; bump it by rebasing the branch onto upstream + re-pinning here.
MLIR_AIE_FORK_COMMIT=fd35e0ca683059a5ad4d5c31968c5df9b41d9999   # atassis/mlir-aie integration-stack = upstream/main 380dcba + #3312 (objectfifo-liveness follow-up) + #3316 (MaterializeBDChains SymbolTable) + #3317 (unplaced-tile diagnostic). Advanced from afc887f-era 8be18cb on 2026-07-11; the 7 O(n^2) build-speed fixes are now MERGED upstream, so the base advance captures them. Build-speed caches/timers go to upstream PRs, not carried. Prev pin 8be18cb (8 build-speed keepers on afc887f)
MLIR_DISTRO_WHEEL=mlir-23.0.0.2026060107+068c6c5c              # MLIR core = LLVM 068c6c5c (upstream/main mlir-aie's pin); prebuilt wheel fetched from the Xilinx/mlir-aie `mlir-distro` release + content-addressed cache by toolchain_up. Bump the MLIR core = change this one line. Replaces the hand-built mlir-aie/my_install/mlir dir (had no HasAncestor -> too old for latest mlir-aie).
PEANO_DIST=llvm_aie-21.0.0.2026062301+cb664e8c                 # pinned Peano (.venv-iron wheel); bumped from 2026052701 to upstream afc887f blessed nightly; VALIDATED: decode WER 0.0842 == old-Peano baseline (prod-binary A/B, 17 clips, identical to 4 decimals)
NANOBIND=2.12.0                                                # the dep that silently broke us at 2.13.0
