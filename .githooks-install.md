# Install the pre-push guard

Run once after cloning:

    git config core.hooksPath hooks

This activates `hooks/pre-push`, which blocks any push that adds a private
path, an absolute home path, or agent-generated commit-message text.
