diff --git a/CMakeLists.txt b/CMakeLists.txt
index cae6e45..fd89875 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -349,10 +349,17 @@ endif()
 
 
 # Last because each of these has its own CMakeLists.txt which reloads/re-finds LLVM, thus resettings globals.
-add_subdirectory(programming_examples)
-add_subdirectory(programming_guide)
-add_subdirectory(test)
-add_subdirectory(mlir_exercises)
+# These four are tests/examples only; their check-* lit targets depend on AIEPythonModules, so they
+# cannot be configured when AIE_ENABLE_BINDINGS_PYTHON=OFF. Gate them so the compiler (aiecc) can be
+# built standalone without the Python bindings. Default ON preserves upstream behavior.
+option(AIE_INCLUDE_TESTS_AND_EXAMPLES
+       "Build the test/example/tutorial subdirectories (need Python bindings)" ON)
+if(AIE_INCLUDE_TESTS_AND_EXAMPLES)
+  add_subdirectory(programming_examples)
+  add_subdirectory(programming_guide)
+  add_subdirectory(test)
+  add_subdirectory(mlir_exercises)
+endif()
 add_subdirectory(cmake/modules)
 
 get_filename_component(COMPILER_DIRECTORY ${CMAKE_CXX_COMPILER} DIRECTORY)
