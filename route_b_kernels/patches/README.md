# Vendored toolchain patches (pinned + tethered)

Out-of-tree patches against the external AMD toolchains we build on, carried here as the durable
"pinned + tethered patch" artifact (upstream PRs are deferred until the service is finished — owner call).
Each patch names the upstream repo + the pinned commit it applies against.

> STATUS (2026-07-12): the mlir-aie **build-speed** rows below are SUPERSEDED by the fd35e0ca toolchain
> advance and are NO LONGER carried. The O(n^2)/lowering fixes (materialize-lower-once, slice-strip,
> datawords-cache, shimdma-symbol-cache, MaterializeBDChains) MERGED upstream (#3178/#3211/#3212/#3216/#3316)
> and are present in the pinned toolchain. The remaining caches/timers (core-elf-cache, compilecores-kernel-
> cache, aiecc pass/phase timers) were re-checked and measured marginal on the post-merge base (core-elf-cache
> 0 wall at B=128; kernel-cache-dedup ~1-3s + needs a rewrite; timers diagnostic-only) -- they are parked for a
> fresh look, with source on fork branch xdna2-asr. These rows are retained as the design record. The IRON and
> mlir-air rows remain live.

| Patch | Upstream repo | Pinned at | What it does |
|---|---|---|---|
| `mlir-aie-cachyos.patch` | `mlir-aie` submodule | (submodule SHA) | bf16 BFP16 fast-path microkernel + CachyOS build fixes |
| `mlir-aie-aiecc-jobs.patch` | `mlir-aie` submodule | (submodule SHA) | Adds `-j${AIECC_JOBS}` to the `make`-based whole_array aiecc invocation (`matrix_multiplication/makefile-common`), parallelizing per-core compilation. Mirrors the IRON full-elf `AIECC_JOBS` lever (`iron-aiecc-build-perf.patch`) for the make path Parakeet builds on, which IRON's `base.py` patch can't reach. Default `AIECC_JOBS?=1` = sequential = no behaviour change; correctness-neutral (parallelism doesn't change xclbin bytes). Applies AFTER cachyos (shares makefile-common). |
| `iron-transpose-num-batches.patch` | `$WS/amd/IRON` | `5503a95` | Adds `num_batches` to the IRON `Transpose` operator (head-batches B contiguous (M,N) transposes into ONE dispatch, mirroring GEMV's batching). Backward-compatible: `num_batches=1` (the default) is behaviorally identical, so existing callers are unaffected. **Also adds opt-in `coalesce_batch_dma` — ⚠️ ON-DEVICE FALSIFIED (do NOT enable `COALESCE_TR`):** the coalesce shrinks dma_bd 3072→2/4 per transpose and builds faster, BUT the decode is numerically WRONG (production WER 1.0 vs baseline 0.1245) — the L2L1 `.forward()` stream-reshape breaks under the single coalesced fill. Kept opt-in/default-off (generator warns loudly) for a future fix; see `internal notes`. The `num_batches` support and the GEMV coalesce are unaffected. Original (now-falsified) description: (B-unroll→BD-iteration, same as the GEMV lever): for the SINGLE-TILE config (num_columns==num_channels==1, m==M, n==N) where each per-batch L3 transfer is a contiguous run of M*N, it collapses the per-batch fill/drain unroll into ONE batched 4D BD (batch in the size-uncapped dim, run split ≤1023) + drops the per-batch `wait` (→ ObjectFifo backpressure). **dma_bd 3072→2 per transpose.** Offline access-equivalent (`scripts/tap_equivalence_transpose.py`). Default off (`field(repr=False)` → op name/cache key unchanged); multi-tile / multi-column requests fall back to the per-batch path (the contiguous-run assumption only holds single-tile, e.g. decode S≤128/TP≤128; production S=448 transpose is multi-tile → NOT covered yet). The decode opts in via `COALESCE_TR=1`. |
| `iron-gemm-fusion-prefix.patch` | `$WS/amd/IRON` | `devel` | Makes the IRON `GEMM` operator fusable under `FusedMLIROperator`: applies the injected `func_prefix` to BOTH the kernel-object filename and the kernel **symbol** names (`zero_*`, `matmul_*`, `convert_copy_*`) in `gemm/design.py`, matching the fusion layer's per-op `op{idx}_` filename + `prefix_symbols` rename. GEMV already did this; GEMM never did because deep-C never fused a GEMM. Backward-compatible: with `func_prefix=""` (non-fused) the strings are unchanged. |
| `mlir-aie-aiecc-materialize-lower-once.patch` | `mlir-aie` submodule | (submodule SHA) | **Build-speed lever (−57% aiecc wall, byte-identical).** Eliminates O(devices²) redundant re-lowering: the full-ELF/no-expand path called `generateNpuInstructions` per device, each cloning the WHOLE 21-device module and re-running the module-level runtime-sequence materialize. Replaced with `generateAllNpuInstructionsOnce` — clone+lower the whole module ONCE after the per-device compile loop, then translate every device's runtime sequence (`AIETranslateNpuToBinary`, the only device-specific step). Gated by `generateFullElf && !expandLoadPdis` (other paths unchanged). **Byte-identical** (materialize is module-level + PDI-id assignment is whole-module → one lowering == N per-device lowerings): frozen-MLIR B=128 L1 ELF sha `370686d` unchanged. Materialize phase 659s(21×)→170s(1×); aiecc 855s→363s. Stacks on (applies AFTER) `mlir-aie-aiecc-phase-timers.patch`. See [[aiecc-materialize-lower-once-result]]. |
| `iron-gemv-coalesce-batch-dma.patch` | `$WS/amd/IRON` | `devel` | **Op-count lever (B-unroll→BD-iteration), opt-in.** Adds `coalesce_batch_dma` to the IRON GEMV (`op.py` field `repr=False` + `design.py`): when set, the per-batch A-fill/C-drain DMA unroll (`num_batches` 1D TAPs + `num_batches` task-groups) collapses into ONE batched 4D TAP per column (layout `[1, num_batches, run_hi, run_lo]`: batch in the size-uncapped hw dim, the contiguous run split across the two wrap dims ≤1023, satisfying the AIE2p caps: wrap size ≤1023, stride ≤2²⁰, iter ≤64) + a single task-group. **dma_bd 105,150→6,910 (~15×) on the B=128 fused decode.** Offline access-equivalent (`scripts/tap_equivalence_check.py` — identical DRAM index sequence). Default off → byte-identical (`370686d`, regression-confirmed); the decode opts in via `COALESCE_GEMV=1`. **On-device WER VALIDATED: B=16 L12 production decode = 0.1245 == unrolled baseline** (the change removes the per-batch `wait` sync → relies on ObjectFifo backpressure; correct on hardware). See `internal notes`. |
| `mlir-aie-aiecc-core-elf-cache.patch` | `mlir-aie` submodule | `8373e49` | **Build-speed lever (opt-in `CORE_CACHE_DIR`, byte-identical).** In `peanoLlcAndLink`, content-addresses the final LINKED per-core `.elf` by `sha256(.opt.ll + ld.script + linkFiles + target + optLevel)`: on a cache hit, copies the cached `.elf` and skips `llc`+`link`; on miss, compiles + stores. Lets a fixed-config REBUILD reuse compiled cores (ccache-style). Default unset = unchanged. The whole-array ELF assembly still runs + the byte gate validates every build → a stale/colliding hit can't ship silently. **Byte-identical** (warm K=1 block sha `f26c131` unchanged). Warm block 11s→**9.7-10.1s**; cut per-core 4.36s→3.29s (the `llc`+`link` wall; the per-core remainder is representative clang+opt → a `.opt.ll` cross-build cache is the next layer for solidly <10s). See [[kernel-cache-warm-build-result]]. |
| `mlir-aie-shimdma-symbol-cache.patch` | `mlir-aie` submodule | `8373e49` | **Build-speed lever (−136s aiecc, byte-identical).** `AIESubstituteShimDMAAllocations` resolved each task's shim-DMA allocation via `ShimDMAAllocationOp::getForSymbol` → `device.lookupSymbol` (a linear symbol-table scan) **once per `DMAConfigureTaskForOp`** → O(tasks × allocations) = O(n²) (per-pass scaling: 40s@L6 → 156s@L12 = 3.86× for 2× layers). Builds a `mlir::SymbolTable` for the device ONCE in `runOnOperation` (O(1) lookups); the pattern never creates/erases symbols so the table stays valid across the greedy run. **Byte-identical** (frozen B=128 L12 S=448 ELF sha `16b9cd9b` unchanged). Pass **155.8s→14.9s (10.5×)**; aiecc wall 483s→347s; full prod build ~536s→~400s (~6.7min). Independent file (no stacking). Upstream-worthy generic fix (not decode-specific). Same `getForSymbol` O(n²) also in `AIEMaterializeBDChains` (~8.5s) — fix candidate. See [[shimdma-symbol-cache-result]]. |
| `mlir-aie-aietargetnpu-datawords-cache.patch` | `mlir-aie` submodule | (submodule SHA) | **KEYSTONE build-speed lever (−103s, byte-identical).** `NpuBlockWriteOp::getDataWords()` resolved its data `memref.global` via `device.lookupSymbol` (linear `SymbolTable::lookupSymbolIn` scan) + `global.getInitialValue()` on EVERY block-write → O(block-writes × globals); perf showed this (`getInherentAttr` 36.6% + `lookupSymbolIn` 10.4%) was ~47% of the translate. Adds `cachedBlockWriteData` in `lib/Targets/AIETargetNPU.cpp`: per-device `SymbolTable` built ONCE (O(1) lookup) + per-global memoized `DenseIntElementsAttr`. **Byte-identical** (B=128 L1 sha `370686d` unchanged; falls back to canonical `getDataWords()` on miss). npu-insts 124s→21.4s; aiecc ~150s→~38s. Independent file (no stacking). See [[aiecc-translate-datawords-cache-result]]. |
| `mlir-aie-aiecc-slice-strip-runtimeseq.patch` | `mlir-aie` submodule | (submodule SHA) | **Build-speed lever (per-core −139s, byte-identical).** In `compileCores`' slice-once loop, also erase every `aie.runtime_sequence` from each per-core slice (it previously kept all 21 devices' runtime sequences — ~640k lines, e.g. a GEMV device's runtime seq = 24,584 `dma_bd` ops — re-parsed by all 212 workers). The per-core object never references the runtime sequence (host-side DMA orchestration), so byte-identical: frozen B=128 L1 sha `370686d` unchanged. per-core-compile 183.4s→44.0s. Stacks on the slice-once/kernel-cache/lower-once stack. See [[aiecc-slice-strip-result]]. |
| `mlir-aie-aiecc-pass-timing.patch` | `mlir-aie` submodule | (submodule SHA) | **Byte-neutral profiling.** Enables `PassManager::enableTiming()` on the NPU-lowering pipeline when `AIECC_PASS_TIMING` is set — per-pass MLIR timing to locate the npu-lowering wall. Used to find that the pass pipeline is only ~24s (materialize-greedy 17.7s) while ~106s is `AIETranslateNpuToBinary` ([[aiecc-materialize-lower-once-result]]). Stacks on `mlir-aie-aiecc-materialize-lower-once.patch`. |
| `mlir-aie-aiecc-phase-timers.patch` | `mlir-aie` submodule | (submodule SHA) | **Byte-neutral profiling instrumentation** for the build-chain attack. Adds `std::chrono` wall-clock phase timers in `tools/aiecc/aiecc.cpp` around placement / resource-allocation / routing / per-core-compile / npu-insts+materialize / elf-from-insts / cdo / full-elf-assembly (compileAIEModule) + parse (processInputFile), summed across the serial device loop with a summary table. Env-gated: `AIECC_PHASE_TIMERS=1` enables; `AIECC_PHASE_TIMERS_FILE=<path>` adds a file sink (IRON swallows aiecc stdout). **Zero effect on emitted bytes** (only prints). Stacks on (applies AFTER) `mlir-aie-compilecores-kernel-cache.patch` — timer hunks are in functions disjoint from the kernel-cache hunks. Used to produce the definitive phase breakdown [[aiecc-phase-breakdown-profile]] (materialize 77%, per-core 22%, assembly <1%). |
| `iron-ops-trace.patch` | `$WS/amd/IRON` | `devel` (applies AFTER the other `iron-*` patches) | **Per-op on-NPU hardware trace wiring (measurement-only, gated OFF by default → production byte-identical).** Adds `rt.enable_trace(trace_size, workers=[…][:IRON_TRACE_NTILES], coretile_events=[PORT_RUNNING_0/1/2 + INSTR_EVENT_0/1 + MEMORY_STALL + LOCK_STALL + INSTR_VECTOR])` to 5 op designs (`channeled_unary_design` = LayerNorm/GELU/SiLU/…, `gemv`, `softmax`, `transpose`, `gemm`). These designs accepted `trace_size` (or nothing) but **never called `enable_trace`**, so the IRON program emitted no `aie.trace` config ops → trace.txt came back all-zeros (the original BLOCKER C). Gated: `channeled_unary` honors the `trace_size` param OR `IRON_TRACE_SIZE` env; the other four honor `IRON_TRACE_SIZE` env only. **No-op when unset → production builds unaffected.** `IRON_TRACE_NTILES` (default 1) traces only a representative tile to avoid the multi-column trace-flow routing conflict (`aie.masterset … same destination` — the 8-col GEMV saturates all shim DMA channels). Drives `scripts/perop_trace_measure.py`. Upstreamable parity (bring `enable_trace` to the ops that lack it). |
| `mlir-aie-compilecores-kernel-cache.patch` | `mlir-aie` submodule | (submodule SHA) | Kernel-cache dedup in aiecc `compileCores` (Peano/`--no-xbridge` path). Keys each core by a structural hash of its masked core BODY; per device, fully compiles ONE representative per key (Wave A) and DERIVES the rest (Wave B) by renaming the rep's `.opt.ll` symbols to this tile's (core/tile names + per-tile buffer/lock globals enumerated from the device shell) then running only the cheap llc+link with this tile's `.ld.script`. **Byte-identical to a from-scratch compile by construction — B=128 L1 ELF sha `1e6098a3` matches the reference.** Falls back to full compile if the rename map can't be built (count/masked-form mismatch) — correctness never at risk. Stacks on (applies AFTER) `mlir-aie-compilecores-slice-once.patch`. Per-device dedup: 212 cores → 17 full compiles (~12.5×). NOTE: the per-core compile is a small slice of the B=128 wall (materialize/assembly-bound), so the build-time win on this design is modest — the cache's payoff is on per-core-bound / repeated builds. See [[kernel-cache-dedup-result]]. |

## `iron-transpose-num-batches.patch` — apply

```bash
cd $WS/amd/IRON          # pinned at 5503a95
git apply /path/to/route_b_kernels/patches/iron-transpose-num-batches.patch
```

**Why:** lever #3 ([[lever3-dispatch-overhead-bound]]) — the fused Whisper decode dispatch is
launch-overhead-bound, and the per-head V transposes were ~47% of the ~612 micro-launches/token because
`Transpose` (unlike `GEMV`/`StridedCopy`) couldn't batch over heads, forcing a Python unroll over H=12.
With `num_batches=H` the 12 self-attn transposes collapse to 1 launch/layer. Touches
`iron/operators/transpose/{op.py,design.py}` only; the `.cc` kernel (transposes s×s sub-tiles) is unchanged.

**Validation status:** **builds clean on the NEW mlir-aie 1.3.2 stack** (12-layer coalesced resident ELF
via `scripts/build_deepc_decode.sh`; scratch 310→281.7 MB = the 12× `vcTc` cross-transpose buffers removed,
confirming the coalescing took effect). On-device numerical (rel-L2 via `verify_fused_decode_sp.py`) + WER
(gate 0.1172) + dispatch-timing A/B is PENDING the single-tenant NPU (see [[lever3-dispatch-overhead-bound]]
"Next" + `scripts/lever3_coalesce_ab.sh`).

**Stack note (2026-06-16, post-deep-C):** deep-C landed the migration to the new vendored mlir-aie 1.3.2 +
constant resident ELF ([[deepc-resident-scratchpad-result]]). This `num_batches` change **ported forward
cleanly**: it is orthogonal to `amd-IRON-deepc.patch` (which doesn't touch `transpose`), so both apply to the
same `amd/IRON` checkout. `build_deepc_decode.sh` applies the deep-C patch; this transpose patch is applied
on top (the lever-3 A/B script does so idempotently). The old `ironenv` 0.0.1 is retired. **Durable carry:**
fold this into the deep-C IRON patch set when convenient (owner call); for now it is a separate tethered patch.

## `iron-gemm-fusion-prefix.patch` — apply

```bash
cd $WS/amd/IRON
git apply /path/to/route_b_kernels/patches/iron-gemm-fusion-prefix.patch
```

**Why:** lever #3 vector-(b) batching needs the IRON `GEMM` op (skinny-N `out[M,N]=W[M,K]@X[K,N]`) inside a
fused full ELF. On the new stack, fusing a GEMM failed two ways: (1) the `link_with` kernel-object name was
left unprefixed while fusion renamed the `.o` to `op0_…` → "could not copy gemm_…o"; (2) the matmul/zero
kernel **symbols** were unprefixed while `prefix_symbols="op0_"` renamed them in the object → `undefined symbol:
matmul_bf16_bf16`. The fix prepends `func_prefix` in both places (GEMV's `op.py`/`design.py` already do this).
Orthogonal to the deep-C + transpose patches; all three apply to the same checkout. `scripts/build_gemm_probe.sh`
applies it idempotently. **Validation:** the N∈{16,32,64,128} fc1-GEMM probe ELFs build clean (compile-only);
on-device correctness (rel-L2 ≤ 0.08 via `fused_elf_probe`) + the dispatch-amortisation sweep are PENDING the
single-tenant NPU (Milestone 0 of `internal notes`).
