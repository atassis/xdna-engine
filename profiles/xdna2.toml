# Resolved tuning for the XDNA2 hardware class. Committed = same for all installs of this class
# (Class 0/1). Omitted keys keep the baked default in npu_asr::tuning::TuningConfig::baked_default.
# Finalized by `probe_tuning` (see internal notes).
# Provisional values below mirror the measured Class-0 winners.
modal_epilogue   = true
subsample_on_npu = true
glu_fused        = true
mm2_pipeline     = true
int8_fast_epi    = true
# TBD-by-harness (left at baked default until probe_tuning confirms):
# layernorm_on_npu = false
# qkv_overlap      = false   # harness may set true on native
# int8_onchip_dequant = false
# Class-3 crossover (recorded; per-request dispatch wired in serving spec B):
# cpu_npu_crossover_s = 20.0
